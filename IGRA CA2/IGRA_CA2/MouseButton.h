#pragma once

// An enumeration mapped to the buttons on a mouse.
// This enumeration only have mappings for the left, middle and right mouse buttons.
enum class MouseButton
{
    Left,
    Middle,
    Right
};
