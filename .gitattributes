# Specify file types to be used with Git LFS
#   - filter=lfs tells Git to use this file with LFS
#   - diff=lfs tells Git how to generate diffs for this file
#   - merge=lfs tells Git how to merge (mostly it is just accept mine or theirs)
#   - -text tells Git to NOT normalize line endings for the file
*.jpg filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text

# These files are text and should be normalized
# (converted from CRLF to LF)
*.cpp text
*.h text
*.sln text
*.vcxproj text
*.vcxproj.filters text
